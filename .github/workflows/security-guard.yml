name: Security Guardrails

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  guardrails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run repository secret guardrails
        run: bash scripts/security_guardrails.sh
